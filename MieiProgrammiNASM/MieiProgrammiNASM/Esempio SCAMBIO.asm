SECTION .DATA
	STR DB "roma"
	n EQU $-STR
	
SECTION .TEXT
GLOBAL START
START:
	;INIZIALIZZAZIONE
	XOR ESI, ESI
	XOR EDI, n-1
CICLO:
	CMP ESI, EDI
	JGE FINE
	MOV AL, [STR+ESI]
	XCHG AL, [STR+EDI]
	MOV [STR+ESI], AL
	INC ESI
	DEC EDI
	JMP CICLO

FINE:
	PRINTB STR, n
	EXIT 0
	